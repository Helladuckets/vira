<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Talk to the paired hub over the tailnet (plain HTTP inside the
         encrypted Tailscale tunnel — see network_security_config). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- SMS history backfill + live SMS capture. -->
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <!-- Vira's pings arrive as local notifications. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Scanning the pairing QR. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera.any"
        android:required="false" />

    <application
        android:label="Vira Companion"
        android:icon="@drawable/ic_vira"
        android:theme="@style/Theme.ViraCompanion"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="false">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".PairActivity"
            android:exported="false" />

        <!-- Live SMS as it arrives (history comes from the provider). -->
        <receiver
            android:name=".SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- RCS and WhatsApp have no readable message store; their
             notifications are the only door. Also hosts the ping
             long-poll, so pings work while the app is closed. -->
        <service
            android:name=".NotificationCapture"
            android:exported="false"
            android:label="Vira Companion"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>
</manifest>
